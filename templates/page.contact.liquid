<!-- <h1>{{ page.title }}</h1>
{% form 'contact' %}
  {% if form.posted_successfully? %} Thank you for your contact {% endif %}
  {{ form.errors | default_errors }}
  <label for="ContactFormName">name</label>
  <input type="text" id="ContactFormName" name="contact[name]">
  <label for="ContactFormEmail">email</label>
  <input type="email" id="ContactFormEmail" name="contact[email]">
  <label for="ContactFormPhone">phone</label>
  <input type="tel" id="ContactFormPhone" name="contact[phone]">
  <label for="ContactFormMessage">message</label>
  <textarea rows="10" id="ContactFormMessage" name="contact[body]"></textarea>
  <input type="submit" value="send">
{% endform %} -->

{% form 'contact' %}

<section class="w-full mt-12 mb-12 max-w-2xl px-6 py-4 mx-auto bg-white rounded-md shadow-md dark:bg-gray-800">
<div class="text-center w-full ">    {% if form.posted_successfully? %} <span class="bg-green-400 rounded w-full mb-8 py-2 px-4">Merci pour votre message !</span> {% endif %}
    {{ form.errors | default_errors }}</div>
  <h2 class="text-3xl font-semibold text-center text-custom-grayBackground ">{{ page.title }}</h2>
  <p class="mt-3 text-center text-gray-600 dark:text-gray-400">Le support SAV est ouvert du lundi au vendredi et de 9h à 17h. Nos délais de réponses sont de deux jours ouvrés maximum.</p>
  
  
  <div class="mt-6 ">
      <div class="items-center -mx-2 md:flex">
          <div class="w-full mx-2">
              <label for="ContactFormName"class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-200">Name</label>

              <input class="block w-full px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring" type="text" id="ContactFormName" name="contact[name]">
          </div>

          <div class="w-full mx-2 mt-4 md:mt-0">
              <label for="ContactFormEmail" class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-200">E-mail</label>

              <input type="email" id="ContactFormEmail" name="contact[email]" class="block w-full px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
          </div>
          <div class="w-full mx-2 mt-4 md:mt-0">
              <label for="ContactFormPhone" class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-200">Téléphone</label>

              <input type="number" id="ContactFormPhone" name="contact[phone]" class="block w-full px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring">
          </div>
      </div>

      <div class="w-full mt-4">
          <label for='ContactFormMessage' class="block mb-2 text-sm font-medium text-gray-600 dark:text-gray-200">Message</label>

          <textarea id="ContactFormMessage" name="contact[body]" class="block w-full h-40 px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-md dark:bg-gray-800 dark:text-gray-300 dark:border-gray-600 focus:border-blue-500 dark:focus:border-blue-500 focus:outline-none focus:ring"></textarea>
      </div>

      <div class="flex justify-center mt-6">
          <input  value="Envoyer" type="submit" class="px-4 py-2 cursor-pointer text-white transition-colors duration-200 transform bg-gray-700 rounded-md hover:bg-gray-600 focus:outline-none focus:bg-gray-600">
      </div>
  </div>
</section>
{% endform %} 